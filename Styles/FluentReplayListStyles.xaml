<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="FluentReplayCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>

    <Style x:Key="FluentReplayColumnHeaderPanelStyle" TargetType="Grid">
        <Setter Property="Padding" Value="12,10,12,8" />
        <Setter Property="ColumnSpacing" Value="12" />
        <Setter Property="Background" Value="{ThemeResource LayerFillColorDefaultBrush}" />
    </Style>

    <Style x:Key="FluentReplayListStyle" TargetType="ListView">
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="IsItemClickEnabled" Value="True" />
        <Setter Property="SingleSelectionFollowsFocus" Value="False" />
        <Setter Property="Padding" Value="0,4,0,8" />
        <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ItemContainerTransitions">
            <Setter.Value>
                <TransitionCollection>
                    <AddDeleteThemeTransition />
                    <ReorderThemeTransition />
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FluentReplayListItemStyle" TargetType="ListViewItem">
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="FluentReplayRowTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="IsTextSelectionEnabled" Value="False" />
    </Style>

    <GridLength x:Key="ReplayColTimeWidth">150</GridLength>
    <GridLength x:Key="ReplayColMatchIdWidth">200</GridLength>
    <GridLength x:Key="ReplayColSlotMatchIdWidth">150</GridLength>
    <GridLength x:Key="ReplayColVersionWidth">72</GridLength>
    <GridLength x:Key="ReplayColSlotVersionWidth">60</GridLength>
    <GridLength x:Key="ReplayColUserIdWidth">60</GridLength>
    <GridLength x:Key="ReplayColImportedWidth">72</GridLength>
    <GridLength x:Key="ReplayColActionWidth">40</GridLength>
    <GridLength x:Key="ReplayColFlexWidth">*</GridLength>

</ResourceDictionary>
